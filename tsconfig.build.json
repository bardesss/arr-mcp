// Build-only config: emits src/ and nothing else, so the production image does
// not ship the test suite or the maintainer-run scripts. `tsconfig.json`
// still includes test/ and scripts/ so `npm run typecheck` and the editor
// cover both.
{
  "extends": "./tsconfig.json",
  "include": ["src"]
}
